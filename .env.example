# Environment Variables

Copy this file to `.env.local` for local secrets if needed later.

```bash
cp .env.example .env.local
```

Currently this project does not require API keys for core features.
WhatsApp links and content are configured in source data files.

# Example (for future use)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
